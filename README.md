# Ansible-shadow

Use ansible to automatically setup shadow

## Prerequiste
In `main.yml`, set 
- Password
- Domain name

## Usage
`sudo whoami`
> To cache the sudo token

`ansible-playbook main.yml`


